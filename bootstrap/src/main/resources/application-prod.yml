logging:
  level:
    root: INFO
    com.demo: INFO
    com.demo.ordercore.infrastructure.repository.mysql.mapper: WARN

# 生产环境配置
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

# Redis 连接配置（示例）
# spring:
#   data:
#     redis:
#       host: prod-redis-server
#       port: 6379
#       database: 0
#       password: ${REDIS_PASSWORD}
#       lettuce:
#         pool:
#           max-active: 50
#           max-idle: 20
#           min-idle: 10
#       timeout: 3000ms

# AWS SQS 配置（示例）
# aws:
#   sqs:
#     region: us-east-1
#     queue-url: https://sqs.us-east-1.amazonaws.com/123456789/order-core-prod

# 链路追踪采样率调整（生产环境可降低采样率）
management:
  tracing:
    sampling:
      probability: 0.1
