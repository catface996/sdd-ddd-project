logging:
  level:
    root: INFO
    com.catface: INFO

# 生产环境配置
# 数据库连接配置（示例）
# spring:
#   datasource:
#     url: jdbc:mysql://prod-mysql-server:3306/order_core_prod?useUnicode=true&characterEncoding=utf8&useSSL=true
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#     hikari:
#       maximum-pool-size: 50
#       minimum-idle: 20
#       connection-timeout: 30000
#       idle-timeout: 600000
#       max-lifetime: 1800000

# Redis 连接配置（示例）
# spring:
#   data:
#     redis:
#       host: prod-redis-server
#       port: 6379
#       database: 0
#       password: ${REDIS_PASSWORD}
#       lettuce:
#         pool:
#           max-active: 50
#           max-idle: 20
#           min-idle: 10
#       timeout: 3000ms

# AWS SQS 配置（示例）
# aws:
#   sqs:
#     region: us-east-1
#     queue-url: https://sqs.us-east-1.amazonaws.com/123456789/order-core-prod

# 链路追踪采样率调整（生产环境可降低采样率）
management:
  tracing:
    sampling:
      probability: 0.1
