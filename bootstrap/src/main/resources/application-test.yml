logging:
  level:
    com.catface: DEBUG

# 测试环境配置
# 数据库连接配置（示例）
# spring:
#   datasource:
#     url: jdbc:mysql://test-mysql-server:3306/order_core_test?useUnicode=true&characterEncoding=utf8&useSSL=false
#     username: ${DB_USERNAME}
#     password: ${DB_PASSWORD}
#     hikari:
#       maximum-pool-size: 10
#       minimum-idle: 5
#       connection-timeout: 30000

# Redis 连接配置（示例）
# spring:
#   data:
#     redis:
#       host: test-redis-server
#       port: 6379
#       database: 0
#       password: ${REDIS_PASSWORD}

# AWS SQS 配置（示例）
# aws:
#   sqs:
#     region: us-east-1
#     queue-url: https://sqs.us-east-1.amazonaws.com/123456789/order-core-test
